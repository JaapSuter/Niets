package com.whatsapp;

import android.os.Handler;
import android.os.Message;
import java.util.HashMap;
import java.util.TimerTask;

class fab extends TimerTask
{
  final hz a;

  public fab(hz paramhz)
  {
  }

  public void run()
  {
    if (hz.a(this.a));
    while (true)
    {
      return;
      this.a.f = true;
      if (!this.a.k)
        hz.l.obtainMessage(2, this.a.a).sendToTarget();
      hz.a().remove(hz.b(this.a).toString());
    }
  }
}